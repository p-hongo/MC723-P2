The files of this benchmark are organized in directories as follow:
  bin: executable files
  data: files containing the correct results, which can be compared to the files in the output directory
  input: input files to the applications
  output: output files generated by the applications

Qsort Small:
   To run: bin/qsort_small input/input_small.dat > output/output_small.txt
   To check the results: diff --brief --report-identical-file data/output_small.txt output/output_small.txt
Qsort Large:
   To run: bin/qsort_large input/input_large.dat > output/output_large.txt
   To check the results: diff --brief --report-identical-file data/output_large.txt output/output_large.txt
